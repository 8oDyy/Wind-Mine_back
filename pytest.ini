[pytest]
# La CI et `pytest` local ne collectent QUE le dossier tests/.
# (test_wine_endpoints.py à la racine est un script d'intégration manuel
#  qui appelle un serveur réel — il ne doit pas être ramassé automatiquement.)
testpaths = tests
